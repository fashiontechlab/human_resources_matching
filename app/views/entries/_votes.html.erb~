<div class="votes">
  <% @entry.voters.oder("votes.create_at").each do |voter| %>
  ☆<%= vorer.name %>
  <% end %>

  <% if current_member && current_member.votable_for?(@entry) %>
  <%= link_to "☆いいね！", [:like, @entry],
      method: :patch, class: "button" %>
  <% end %>
</div>
